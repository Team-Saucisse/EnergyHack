@model Enum
@{
    Type enumType = Model != null ? Model.GetType() : ViewBag.EnumType;
    var list = new SelectList(from object value in Enum.GetValues(enumType) select new { Value = value, Text = value.ToString() }, "Value", "Text", Model);
}
@Html.DropDownList("", list)